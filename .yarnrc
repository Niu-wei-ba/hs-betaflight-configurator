# The local launcher switches to Node 20 before running project tooling.
# Yarn 1 evaluates package engines before it can invoke that launcher, so keep
# this repository-level gate disabled and let scripts/start-local.sh enforce it.
ignore-engines true
