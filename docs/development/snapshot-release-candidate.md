# 2026.6.1 快照发布候选验收

## 基线与范围

- 线上基线：`9844f32a`，`feature/betaflight-2026.6.1`。
- dev 参考：`2b60d165`，只使用已提交版本；原工作树未提交修改保持原样。
- 候选：`codex/snapshot-2026.6.1-release`，从线上基线创建，没有 merge dev。
- `60512d5e` 移植完整快照功能及中文、传感器等共享依赖，共 53 个文件。
- 经直接 tree diff 核对，线上基线已包含 dev 的部署配置、测距 API 1.46 与静态资源修复；移植提交的最终树与 dev 一致。这是两条分支实际差异的结果，不是合并 dev 历史。
- 后续独立修复 API 1.48 传感器字典的键值对构造，避免快照页面取不到传感器类型；更新遗留 v1 promise 测试为 v2 只读请求，不放宽 v2 校验。

## 验证结果（2026-09-11）

- 16 个相关测试文件，135 项通过，覆盖 snapshot、MSP、CLI queue、MSPHelper、battery profile、tab mounter、serial、LED、VTX。
- API 1.48 传感器测试先在原实现失败，再在修复后通过；同时断言快照模式不发送 CLI 命令。
- lint：0 错误，2 条既有 unused variable 警告（LandingTab、BuildApi）。生产构建通过。
- 本地 `http://127.0.0.1:8082/` 使用真实已保存快照 `SUP-CYJ768SY7AKBCVGC`：加载后保留支持快照页，终端 8277 字符；PID/Rates 固定为 1，PID 名称 HQ2521-1，Roll 30/64/31、Pitch 38/81/39；复制、重置、保存禁用。
- LED 页面显示灯珠 0–11，写操作禁用。切回支持快照页后终端保留；退出后菜单恢复、表单与终端清空。
- 线上 API 当前镜像 `bfc_firmware_api:api-78ce048`；只读检查确认 DEFAULT_RETENTION_DAYS=1，规范化客户端期限为 1。
- 容器内 v2 校验器与候选前端校验器 SHA-256 相同：`a3d4cf1d2affd2a674992d644e82648a544cdf9956d71be47bee9bc526a82fe8`。真实历史快照到期时间保持 `2026-10-09T08:21:23.534Z`。

## 交付边界与后续验收

本次只准备本地候选分支，不推送或合入线上分支，不部署。既有真实快照回放不等于重新采集验收；真机两套 Profile 采集、物理断连重连以及全部支持页面逐项真机对照仍待发布前完成。正式合入前重新检查远端线上基线，如发生变化需重放候选提交并重新验证。
