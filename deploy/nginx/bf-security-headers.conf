# Apply anti-clickjacking headers to the entire bf.hs-fpv.com virtual host.
# Expected include location on the server:
#   /www/server/panel/vhost/nginx/extension/bf.hs-fpv.com/*.conf

add_header X-Frame-Options "DENY" always;
add_header Content-Security-Policy "frame-ancestors 'none'" always;
