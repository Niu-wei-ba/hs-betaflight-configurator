import { readFile, writeFile, mkdir } from 'node:fs/promises';
import path from 'node:path';

const outputDir = path.resolve('local-video-ingest/work/screenshot-backfill');
const exportPath = path.resolve('local-video-ingest/work/legacy-backfill/subtitles-export.json');

const plans = {
  BV121wrzSE5k: {
    categories: 'configuration,firmware',
    tags: 'Betaflight,刷写固件,驱动,DFU,BOOT,故障排查',
    chapters: [
      chapter('确认连接异常和设备位置', '在 Windows 设备管理器中找到飞控对应设备或端口。', ['打开“计算机管理”中的设备管理器。', '在“通用串行总线设备”或“端口（COM 和 LPT）”中查找 Betaflight / STM32 相关设备。'], ['设备名称会随飞控型号和驱动状态变化，视频未提供统一名称。']),
      chapter('回退或更新驱动', '让系统重新使用可用的 USB / 串口驱动。', ['右键目标设备，打开“属性”。', '在“驱动程序”页按视频顺序尝试“回退驱动程序”；无效时再选择“更新驱动程序”。', '完成后重新打开 Betaflight 地面站测试连接。'], ['不要随机卸载其他 USB 设备的驱动。']),
      chapter('使用 BOOT 键进入刷写状态', '按视频顺序让飞控进入刷写所需状态。', ['按住飞控上的 BOOT 键。', '保持按住的同时插入 USB。', '再继续后续驱动或固件刷写操作。'], ['实际 BOOT 键位置因飞控而异；操作电机相关项目时应先拆桨。']),
    ],
    body: `## 本教程讲什么

本教程处理刷写固件时“飞控无法被电脑或 Betaflight 地面站识别”的常见情况。视频演示了在 Windows 设备管理器中查找飞控设备、回退或更新驱动，以及按住 BOOT 键再插入 USB 的进入刷写模式方法。

## 适用场景与前置条件

- 适用于飞控已通过 USB 接入电脑，但 Betaflight 地面站无法正常连接或刷写时。
- 需要使用 Windows 设备管理器检查驱动状态。
- 操作前断开动力电池；如需接触电机相关设置，应先拆下桨叶。
- 飞控型号、Windows 版本和具体驱动版本，视频未明确说明。

## 操作步骤

### 1. 在设备管理器中找到飞控设备

1. 在 Windows 中右键“此电脑”，选择“管理”，打开设备管理器。
2. 先在“通用串行总线设备”中寻找与 Betaflight 或 STM32 相关的设备。
3. 如果没有找到，再打开“端口（COM 和 LPT）”，查找以 STM 开头的端口。

### 2. 回退或更新驱动程序

1. 右键目标设备并打开“属性”。
2. 进入“驱动程序”页，按视频顺序先尝试“回退驱动程序”。
3. 回退无效时，选择“更新驱动程序”。
4. 完成后重新打开 Betaflight 地面站，确认是否能正常连接。

### 3. 用 BOOT 键进入刷写状态

1. 按住飞控上的 BOOT 键。
2. 保持按住后插入 USB。
3. 再继续进行驱动安装或固件刷写。

视频指出，未按住 BOOT 键就直接插线刷写，可能导致电脑或地面站无法识别飞控。

## Betaflight 参数与界面说明

- **设备管理器**：用于确认电脑是否把飞控识别为 USB 设备或串口设备。
- **驱动程序页**：视频中用于回退或更新飞控相关驱动。
- **BOOT 键**：用于让飞控进入视频所述的刷写状态；具体位置请以飞控板丝印和厂商说明为准。
- 具体驱动名称、固件目标和刷写选项，视频未明确说明。

## 常见错误与注意事项

- 不要把飞控的驱动问题与其他 USB 设备混淆；只对确认的目标设备操作。
- 更新或回退驱动后应重新连接并重新打开地面站验证。
- BOOT 键操作顺序是先按住，再插入 USB。
- 视频没有给出针对所有飞控型号的通用驱动版本；若问题仍在，请以飞控厂商资料为准。

## 术语表

- **Betaflight 地面站**：用于连接、配置和刷写 Betaflight 飞控的软件。
- **STM32**：视频中提到的飞控主控及其相关设备标识。
- **BOOT 键**：让飞控进入刷写状态的硬件按键。
- **DFU**：常用于 STM32 固件刷写的 USB 设备模式；本视频未展开说明具体配置。

## 视频章节

{{CHAPTERS:BV121wrzSE5k}}

本次使用的是已保存的历史字幕。它不是可靠的官方时间轴，因此不填写估算时间，也不提供虚假的跳转时间。`,
  },
  BV1eqoVY5EXg: {
    categories: 'configuration,firmware',
    tags: 'Betaflight,刷写固件,固件刷写,CRSF,SBUS,OSD,驱动',
    chapters: [
      chapter('读取飞控目标', '在刷写前记录地面站显示的飞控目标。', ['连接飞控并进入地面站。', '记录飞控型号或目标名称。', '若自动识别失败，按已记录的型号手动选择。'], ['型号选择错误会导致刷写失败或配置不匹配。']),
      chapter('选择刷写选项', '根据实际接收机、图传和功能选择刷写选项。', ['启用视频所示的专家模式。', '选择匹配的接收机协议、OSD 类型和所需功能。', '从网络加载固件后再执行刷写。'], ['视频仅给出示例，不应把示例选项当成所有机型的默认值。']),
      chapter('处理 DFU 或串口驱动', '在电脑不能识别刷写设备时修复驱动。', ['使用视频展示的驱动工具安装或修复 DFU 驱动。', '地面站无法连接时，检查 STM32 设备是否被识别为串口。', '修复后刷新网页或重新连接，再重复刷写步骤。'], ['仅修改已确认属于飞控的设备驱动。']),
      chapter('备份并恢复配置', '在刷写前保存原机配置，刷写后按需恢复。', ['拍照保存端口、配置、PID、Rate 和滤波器页面。', '在 CLI 中执行 \`dump\` 并保存输出文件。', '刷写完成后从文件加载配置，执行并输入 \`save\` 保存。'], ['不同固件版本间导入旧配置可能报错，照片可用于人工核对。']),
    ],
    body: `## 本教程讲什么

本教程演示如何在 Betaflight 地面站中刷写飞控固件，并处理目标未自动识别、DFU 驱动缺失和串口驱动异常等问题。视频还强调刷写前备份原机端口、PID、Rate、滤波器和 CLI 配置，刷写完成后再恢复。

## 适用场景与前置条件

- 适用于需要在 Betaflight 地面站中刷写飞控固件的用户。
- 需要能通过 USB 连接飞控，并确认飞控目标名称。
- 刷写前应断开动力电池并拆桨。
- 固件版本、飞控目标、接收机协议和 OSD 类型必须按实际硬件选择；视频未给出通用默认值。

## 操作步骤

### 1. 先记录飞控目标和原机配置

1. 连接飞控，进入 Betaflight 地面站，记录显示的飞控型号或目标名称。
2. 拍照保存端口、配置、陀螺仪角度、PID、Rate 和滤波器页面。
3. 进入 CLI，执行 \`dump\`，把输出保存为文件。

### 2. 进入固件刷写页面并选择目标

1. 使用“激活引导程序”或“更新固件”进入刷写页面。
2. 自动识别成功时核对目标；识别失败时，按第一步记录的目标手动选择。
3. 按视频所示启用专家模式，选择所需固件版本和刷写选项。

### 3. 按实际设备选择协议和功能

1. 接收机协议按实际设备选择：视频以 CRSF 和 SBUS 为例。
2. OSD 类型按模拟图传或数字图传选择。
3. 只勾选实际需要的功能；视频提到 GPS、LED 等功能可按需要补充。
4. 从网络加载固件，加载完成后执行写入。

### 4. 处理 DFU 与串口驱动问题

1. 如果刷写时提示找不到飞控，先检查是否缺少 DFU 驱动。
2. 按视频演示使用驱动工具修复对应的 STM32 驱动，完成后刷新地面站并重新走刷写流程。
3. 如果地面站无法连接，检查电脑是否把飞控识别为 USB 串口设备；视频中建议使用对应驱动工具修复。

### 5. 刷写后恢复配置

1. 刷写完成后重新连接飞控。
2. 在 CLI 中从文件加载之前保存的配置并执行。
3. 输入 \`save\` 保存恢复后的设置。
4. 对照刷写前拍摄的页面，核对关键端口和调参是否正确。

## Betaflight 参数与界面说明

- **飞控目标**：刷写时必须与实际飞控匹配的目标名称。
- **CRSF / SBUS**：视频列举的接收机协议选项，应按实际接收机与图传连接方式选择。
- **OSD 类型**：视频将模拟图传和数字图传作为不同的选择项；具体名称以当前地面站为准。
- **\`dump\`**：CLI 中导出当前配置的命令。
- **\`save\`**：CLI 中保存当前配置的命令。

## 常见错误与注意事项

- 不要在没有记录飞控目标时盲目选择目标；选错可能导致刷写失败或后续配置异常。
- 刷写会清除或改变原有配置，必须先备份，并对关键页面拍照留底。
- 旧配置跨固件版本恢复时可能不完全兼容，遇到报错应逐项检查，不要忽略错误直接飞行。
- 驱动工具只能针对确认属于飞控的 STM32 设备使用。

## 术语表

- **飞控目标**：用于匹配飞控硬件的固件构建目标。
- **DFU**：STM32 刷写常用的 USB 设备模式。
- **CRSF**：视频提到的接收机协议。
- **SBUS**：视频提到的接收机协议。
- **OSD**：FPV 画面叠加显示的数据。

## 视频章节

{{CHAPTERS:BV1eqoVY5EXg}}

本次使用的是已保存的历史字幕。它不是可靠的官方时间轴，因此不填写估算时间，也不提供虚假的跳转时间。`,
  },
  BV1qgV26FEGg: {
    version: 3,
    categories: 'configuration,gps',
    tags: 'Betaflight,GPS,定点悬停,定高,位置保持,高度保持,CRSF,SBUS,Zadig',
    chapters: [
      chapter('备份飞控配置', '在升级前保存当前配置，以便刷写后恢复。', ['连接飞控并进入 CLI。', '执行 \`dump\`。', '把输出保存为文件。'], ['恢复旧配置前应确认其与新固件兼容。']),
      chapter('进入刷写模式并安装驱动', '让飞控以 STM32 Bootloader 状态被电脑识别。', ['按住 BOOT 键后插入 USB。', '打开 Zadig，在设备列表中选择带有 STM32 Bootloader 标识的设备。', '安装驱动后进入 Betaflight 的固件刷写页面。'], ['只有显示 Bootloader 时才是视频所说的刷写状态。']),
      chapter('选择固件功能', '为悬停方案选择接收机、OSD 和所需功能。', ['选择与飞控匹配的目标和视频所示固件版本。', '按实际接收机选择 CRSF 或 SBUS。', '按实际图传选择数字或模拟 OSD，并勾选高度保持、位置保持等实际需要的功能。'], ['视频提到的功能列表和版本是示例，不应直接套用到所有飞控。']),
      chapter('恢复配置并设置悬停参数', '恢复原配置后设置位置保持与高度保持相关参数。', ['在 CLI 中加载备份文件并执行 \`save\`。', '按视频给出的思路设置位置保持和高度保持的相关参数。', '把高度保持与位置保持放在同一遥控器通道，同时启用或关闭。'], ['文中的具体参数范围和命令应以视频、当前固件提示和实际硬件为准。']),
      chapter('室外测试', '在安全的卫星接收条件下验证悬停。', ['确认 GPS 卫星数量充足，视频建议至少八颗。', '先匀速直线飞行一段距离并做较大俯仰动作。', '再尝试开启位置保持和高度保持。'], ['不要在没有确认定位条件时依赖悬停功能。']),
    ],
    body: `## 本教程讲什么

本教程以七寸远航机为例，介绍定点、定高悬停的四个实操环节：备份飞控配置、刷写带位置保持与高度保持功能的固件、在地面站恢复配置并完成相关设置，以及在室外确认 GPS 条件后测试悬停。

## 适用场景与前置条件

- 适用于计划在 Betaflight 飞控上尝试位置保持和高度保持的用户。
- 需要飞控、GPS 及视频所涉及的相关功能已经安装并能被地面站识别。
- 操作前应断开动力电池、拆桨，并备份原配置。
- 飞控目标、固件版本、GPS 型号、罗盘使用方式和实际兼容性，视频未作通用确认。

## 操作步骤

### 1. 备份原有飞控配置

1. 连接飞控，进入 CLI。
2. 输入 \`dump\` 并回车。
3. 将输出保存为文件，以便刷写后恢复。

### 2. 让飞控进入刷写状态并选择固件

1. 按住飞控上的 BOOT 键，再插入 USB。
2. 在驱动工具中确认选择的是带 **STM32 Bootloader** 标识的设备。
3. 打开 Betaflight 固件刷写工具，选择正确的飞控目标。
4. 按实际接收机选择 CRSF 或 SBUS，按实际图传选择数字或模拟 OSD。
5. 根据视频所示功能选择高度保持、位置保持、电子罗盘、LED 和舵机等实际需要的项目。
6. 从网络加载固件后开始刷写。

### 3. 恢复配置并完成地面站设置

1. 刷写完成后进入 CLI，从文件加载第一步的备份并执行。
2. 输入 \`save\` 保存恢复后的配置。
3. 视频将位置保持和高度保持的相关参数先设为较低值；如果飞行中位置漂移或高度起伏，再按视频的方向逐步提高。
4. 视频还展示了在不使用磁力计的场景中启用位置保持相关选项的思路；该项必须按实际 GPS、罗盘和当前固件提示确认。
5. 在“模式”页面把高度保持和位置保持分配到同一个遥控器通道，使其同时启用或关闭。

### 4. 在室外测试悬停

1. 起飞前确认 GPS 卫星数量充足；视频建议至少八颗。
2. 先让飞机匀速直线飞行一段距离，并进行较大俯仰动作。
3. 再开启位置保持和高度保持，观察是否能够稳定悬停。
4. 若无法开启，先回到定位条件与飞行动作检查，不要强行依赖自动悬停。

## Betaflight 参数与界面说明

- **位置保持**：视频用于实现自动定点。
- **高度保持**：视频用于实现自动定高。
- **位置保持 + 高度保持**：视频将两者同时开启，作为定点定高悬停方案。
- **CRSF / SBUS**：应按实际接收机、图传与飞控连接方式选择。
- **CLI 的 \`dump\` / \`save\`**：分别用于导出配置和保存配置。
- 具体 CLI 参数名称、允许范围和固件版本，视频未明确给出可通用复用的完整清单。

## 常见错误与注意事项

- 恢复旧配置前先确认它与新固件兼容；报错时不要跳过检查。
- 进入刷写模式时应确认设备名称包含 Bootloader。
- 高度保持和位置保持依赖实际传感器与定位条件，不能只靠开关配置就保证可用。
- 视频建议在卫星数充足并完成必要飞行动作后再开启悬停；首次测试应在开阔、可控的环境进行。

## 术语表

- **位置保持**：视频中用于自动定点的功能。
- **高度保持**：视频中用于自动定高的功能。
- **GPS**：用于获取卫星定位信息的模块。
- **BOOT 键**：让飞控进入视频所述刷写状态的按键。
- **STM32 Bootloader**：视频中用于确认刷写状态的设备标识。
- **Zadig**：视频中用于安装 STM32 Bootloader 驱动的工具。

## 视频章节

{{CHAPTERS:BV1qgV26FEGg}}

本次使用的是已保存的历史字幕。它不是可靠的官方时间轴，因此不填写估算时间，也不提供虚假的跳转时间。`,
  },
  BV1tUmXB7Eon: {
    categories: 'configuration,firmware,gps',
    tags: 'Betaflight 4.6,固件刷写,高度保持,位置保持,GPS,CRSF,SBUS',
    chapters: [
      chapter('读取飞控目标和当前版本', '刷写前确认飞控目标与当前版本。', ['用现有地面站连接飞控。', '记录飞控目标名称和当前固件版本。', '进入更新固件页面并启用专家模式。'], ['目标必须与实际飞控匹配。']),
      chapter('选择固件和功能', '在刷写页选择目标、版本、接收机协议和所需功能。', ['选择正确飞控目标。', '按实际设备选择 CRSF 或 SBUS。', '按需勾选高度保持和位置保持等功能。', '加载在线固件并选择备份原文件后刷写。'], ['刷写过程中不要断开飞控。']),
      chapter('改用支持该固件的地面站', '使用能连接新固件的地面站继续配置。', ['刷写后先尝试连接。', '若原地面站不支持该版本，按视频改用在线地面站。', '连接后按提示校准加速度计。'], ['可用地面站和兼容范围会随版本变化，视频未给出通用列表。']),
      chapter('配置高度保持和位置保持模式', '把已在刷写时选择的功能配置到遥控器模式。', ['在模式页面找到高度保持和位置保持。', '为两个模式配置遥控器开关范围。', '同时开启两项后，按视频作为定点定高悬停方案使用。'], ['功能出现与否取决于刷写时是否选择及飞控/传感器条件。']),
      chapter('理解时钟同步边界', '区分时钟同步与自动悬停。', ['确认飞控硬件是否支持时钟同步。', '不要把时钟同步与悬停功能绑定为同一个前提。', '旧飞控不支持时，按视频仍可正常使用所述悬停方案。'], ['视频未找到官方的完整时钟同步说明。']),
    ],
    body: `## 本教程讲什么

本教程讲解如何刷写视频所称的 Betaflight 4.6 系列固件、选择高度保持与位置保持相关功能，并在地面站中配置定点定高悬停。视频还说明了原有地面站可能不支持新固件、需要改用在线地面站的情况，以及时钟同步与自动悬停并非同一前提。

## 适用场景与前置条件

- 适用于准备升级 Betaflight 固件、尝试高度保持和位置保持的用户。
- 需要先确认飞控目标、接收机协议、图传类型，以及 GPS 等实际硬件条件。
- 刷写前断开动力电池并拆桨；保存原配置。
- 视频中的版本命名和在线地面站兼容性可能随发布变化，必须以当前官方页面和实际设备为准。

## 操作步骤

### 1. 记录目标和原固件信息

1. 用原有地面站连接飞控。
2. 记录地面站显示的飞控目标和当前固件版本。
3. 进入“更新固件”页面，启用专家模式。

### 2. 选择正确目标、协议和功能

1. 选择与飞控完全一致的目标，避免误刷。
2. 按实际接收机选择 CRSF 或 SBUS。
3. 选择需要的功能；视频以高度保持和位置保持为例。
4. 加载在线固件后，按视频提示选择备份原文件，再确认刷写。
5. 刷写期间不要拔掉飞控。

### 3. 使用可连接新固件的地面站

1. 刷写完成后先尝试重新连接飞控。
2. 如果原地面站提示不支持新固件，按视频改用在线地面站。
3. 连接后按提示校准加速度计，再继续原有配置流程。

### 4. 配置定高、定点与悬停

1. 在“模式”页面查看是否出现高度保持和位置保持功能。
2. 为这两个功能分别配置遥控器开关范围。
3. 同时开启两项时，视频将其作为定点定高悬停方案。
4. 是否可用仍取决于实际传感器、定位条件和飞控支持情况。

### 5. 不把时钟同步和悬停混为一谈

1. 视频认为时钟同步需要相应飞控硬件支持。
2. 视频同时说明，不支持时钟同步并不等于不能使用自动悬停。
3. 视频未给出官方的完整时钟同步说明，不能据此推断具体支持范围。

## Betaflight 参数与界面说明

- **专家模式**：视频用于显示和选择更多刷写选项。
- **高度保持 / 位置保持**：视频在固件刷写时选择，并在“模式”页面配置开关。
- **在线地面站**：当本地地面站不支持新固件时，视频使用的替代方式。
- **时钟同步**：视频称需要飞控硬件支持，但未给出完整官方说明。
- 具体固件版本、传感器型号、功能默认值和硬件兼容性，视频未明确说明。

## 常见错误与注意事项

- 选择错误的飞控目标是刷写风险最高的环节之一，应先记录再选择。
- 本地地面站不能连接新固件，不代表飞控已经损坏；先检查软件兼容性。
- 高度保持与位置保持不是只刷入功能即可安全使用，仍需确认 GPS、传感器和实际飞行条件。
- 不要因旧飞控没有时钟同步就直接判定无法使用视频所述的悬停功能。

## 术语表

- **CRSF / SBUS**：视频提到的接收机协议选项。
- **高度保持**：视频中用于自动定高的功能。
- **位置保持**：视频中用于自动定点的功能。
- **在线地面站**：通过网页打开、用于连接和配置飞控的地面站。
- **时钟同步**：视频讨论的飞控主控与陀螺仪时钟相关能力。

## 视频章节

{{CHAPTERS:BV1tUmXB7Eon}}

本次使用的是已保存的历史字幕。它不是可靠的官方时间轴，因此不填写估算时间，也不提供虚假的跳转时间。`,
  },
  BV1kZbgzsEP1: {
    categories: 'hardware,firmware',
    tags: 'Betaflight 4.6,飞控,ICM-42688-P,陀螺仪,LDO,电调,AM32',
    chapters: [
      chapter('了解时钟同步与陀螺仪建议', '认识视频对飞控硬件更新的前瞻说明。', ['视频讨论主控 MCU 与 ICM-42688-P 外置时钟同步。', '了解飞控设计可能需要接入陀螺仪时钟引脚及独立低噪声供电。', '把性能结论视为前瞻信息，等待实际固件和硬件验证。'], ['视频未提供完整的官方兼容清单或实测结论。']),
      chapter('检查大尺寸飞控', '了解视频展示飞控的接口、接收机和传感器配置。', ['查看接收机天线、黑匣子卡槽、BEC 指示和飞控接口。', '了解板载接收机的天线工作模式。', '核对电机 PWM 输出数量是否满足机架需求。'], ['端口和输出数量取决于具体硬件，不能套用到其他飞控。']),
      chapter('检查电调与装机间距', '识别电调输入范围、焊盘和装机干涉风险。', ['查看电调输入范围和电源、电机焊盘。', '装机时避免飞控 BEC 电感与电调 TVS 二极管挤压。', '让电机线尽量贴合焊盘凹槽，增加接触面积。'], ['焊接前断开电池，避免短路；具体焊接能力应由操作者确认。']),
      chapter('查看 AM32 电调参数示例', '只按视频示例理解电机失步排查方向。', ['用视频展示的工具读取电调信息。', '遇到电机失步时，视频建议检查电机 KV，并尝试调整电机进角和 PWM。'], ['具体参数值和适用电机组合，视频未给出可通用的默认设置。']),
    ],
    body: `## 本教程讲什么

本教程包含两部分：第一部分是对 Betaflight 4.6 相关飞控硬件变化的前瞻，讨论 MCU 与 ICM-42688-P 陀螺仪的时钟同步、独立低噪声 LDO 供电以及陀螺仪选型；第二部分展示一套大尺寸飞控与电调的接口、装机注意点和 AM32 电调参数读取示例。

## 适用场景与前置条件

- 适用于关注 Betaflight 相关飞控硬件、陀螺仪和电调选型的用户。
- 适用于准备安装视频展示的飞控、电调或排查电机失步的用户。
- 装机、焊接和电调调参前必须断开动力电池，并确认具备相应操作能力。
- 视频为前瞻和产品展示，不构成所有飞控、陀螺仪或电调的通用配置建议。

## 操作步骤

### 1. 理解视频讨论的硬件前瞻

1. 视频讨论了飞控 MCU 与 ICM-42688-P 陀螺仪外置时钟同步的可能性。
2. 要实现这类能力，视频认为飞控设计需要接入陀螺仪时钟相关引脚，并使用独立的低噪声 LDO 供电方案。
3. 视频把精度、温漂和稳定性提升作为预期方向；实际效果需等待固件和硬件实测。

### 2. 检查飞控的接口与输出

1. 根据实际装机需求检查接收机天线接口、黑匣子存储卡槽、BEC 状态和外设接口。
2. 视频展示的飞控具有有限数量的电机 PWM 输出；安装前确认它是否满足机架和电机数量。
3. 如使用板载接收机，按视频所示完成对频，并确认天线工作模式是否符合需求。

### 3. 安装电调并避免机械干涉

1. 确认电调的输入电压范围和电源、电机焊盘位置。
2. 安装时避免飞控 BEC 电感与电调 TVS 二极管相互挤压；视频建议调整电调安装方向或留出间距。
3. 焊接电机线时，让导线尽量贴合焊盘中部凹槽，增加接触面积。

### 4. 处理电机失步的示例方向

1. 使用电调配置工具读取当前电调信息。
2. 视频以 AM32 电调为例，遇到失步时先检查电机 KV 是否设置正确。
3. 视频还建议按实际情况尝试调整电机进角和 PWM；不要直接把示例值用于所有电机与电调组合。

## Betaflight 参数与界面说明

- **ICM-42688-P**：视频讨论的陀螺仪型号。
- **MCU 时钟同步**：视频描述的飞控主控与陀螺仪时钟协同能力。
- **LDO**：视频用来说明陀螺仪独立低噪声供电的电路方案。
- **黑匣子**：飞控上的飞行日志存储功能或接口。
- **AM32**：视频展示的电调固件；其参数含义和推荐值需以电调与电机实际情况为准。

## 常见错误与注意事项

- 视频对时钟同步的描述属于前瞻信息，未给出完整官方兼容清单或实测数据。
- 飞控 PWM 输出数量可能限制可驱动电机数量，装机前必须核对。
- 电调与飞控间的机械挤压可能损伤元件，应在最终固定前检查间隙。
- 电机失步的原因不只在进角或 PWM；电机、电调、供电和焊点都应排查。

## 术语表

- **ICM-42688-P**：视频讨论的陀螺仪型号。
- **LDO**：低压差线性稳压器，视频中用于陀螺仪供电说明。
- **BEC**：为飞控或外设提供稳压电源的电路。
- **TVS 二极管**：视频中用于抑制电压冲击的器件。
- **AM32**：视频中电调使用的固件。

## 视频章节

{{CHAPTERS:BV1kZbgzsEP1}}

本次使用的是已保存的历史字幕。它不是可靠的官方时间轴，因此不填写估算时间，也不提供虚假的跳转时间。`,
  },
};

function chapter(title, goal, steps, notes) { return { title, goal, steps, notes }; }
function chaptersMarkdown(id) {
  return plans[id].chapters.map((item, index) => `### ${index + 1}. ${item.title}\n\n- 目标：${item.goal}\n- 操作步骤：${item.steps.join('；')}\n- 注意事项：${item.notes.join('；')}`).join('\n\n');
}
function parseSrt(text) {
  return [...text.matchAll(/\d+\s*\n\d{2}:\d{2}:\d{2},\d{3}\s+-->\s+\d{2}:\d{2}:\d{2},\d{3}\s*\n([\s\S]*?)(?=\n\s*\n\d+\s*\n|$)/g)].map((match) => match[1].replace(/\s+/g, ' ').trim()).filter(Boolean);
}
function calibrate(text) {
  const replacements = [
    [/\bbetaflightstm32f405\b/gi, 'Betaflight STM32F405'], [/创口/g, '串口'], [/创行/g, '串行'],
    [/非空|非控/g, '飞控'], [/Bud键|bud键|bud key/gi, 'BOOT 键'], [/旋庭/g, '悬停'], [/调仓/g, '调参'],
    [/楼盘/g, '罗盘'], [/电条/g, '电调'], [/光流记/g, '光流计'], [/全盘掐除/g, '全盘擦除'],
    [/Sappos|SAPUS|SPAS/gi, 'SBUS'], [/Zedig/gi, 'Zadig'], [/C2SF/gi, 'CRSF'], [/ELS/gi, 'ELRS'], [/D-SHOT/gi, 'DShot'],
    [/上显固件/g, '刷写固件'], [/地面站端/g, '地面站'], [/持电脑/g, '此电脑'],
  ];
  let value = text;
  for (const [pattern, replacement] of replacements) value = value.replace(pattern, replacement);
  return value.replace(/\s+/g, ' ').trim();
}

const entries = JSON.parse(await readFile(exportPath, 'utf8'));
for (const [bvid, plan] of Object.entries(plans)) {
  const item = entries.find((entry) => entry.bvid === bvid);
  if (!item) throw new Error(`Missing preserved subtitle: ${bvid}`);
  const rawLines = parseSrt(item.subtitleContent);
  const calibratedLines = rawLines.map(calibrate).filter((line, index, lines) => line && line !== lines[index - 1]);
  const header = `# ${item.title}\n\n`;
  const markdown = `${header}${plan.body.replace(`{{CHAPTERS:${bvid}}}`, chaptersMarkdown(bvid))}\n\n## 来源\n\n- 视频：${item.title}\n- 作者：${item.authorName}\n- 原视频：https://www.bilibili.com/video/${bvid}/\n`;
  await mkdir(outputDir, { recursive: true });
  await writeFile(path.join(outputDir, `${bvid}.raw-legacy.json`), `${JSON.stringify({ source: 'legacy-whisper', language: 'zh', lines: rawLines.map((text) => ({ text })) }, null, 2)}\n`);
  await writeFile(path.join(outputDir, `${bvid}.calibrated.json`), `${JSON.stringify({ language: 'zh-CN', version: plan.version ?? 2, lines: calibratedLines.map((text) => ({ text })) }, null, 2)}\n`);
  await writeFile(path.join(outputDir, `${bvid}.md`), markdown);
  await writeFile(path.join(outputDir, `${bvid}.chapters.json`), `${JSON.stringify(plan.chapters, null, 2)}\n`);
  console.log(`${bvid}: raw=${rawLines.length}, calibrated=${calibratedLines.length}, chapters=${plan.chapters.length}`);
}
